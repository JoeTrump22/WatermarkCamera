# 🚀 水印相机 - GitHub 在线编译完整教程

> **适用人群**：完全没有 GitHub 经验的新手
> **完成时间**：约 10-15 分钟
> **你将获得**：可以直接安装到手机的 APK 安装包

---

## 📋 准备工作

在开始之前，请确保您的电脑上有：
- ✅ 一个可以上网的电脑（Windows/Mac 均可）
- ✅ 一个有效的电子邮箱地址（用于注册 GitHub）

---

## 第一步：注册 GitHub 账号

1. **打开浏览器**，访问：**https://github.com/signup**
2. **输入您的邮箱地址**（建议使用常用邮箱，如 QQ邮箱、163邮箱）
3. **设置密码**（至少 8 位，包含字母和数字）
4. **设置用户名**（您的 GitHub 名字，只能用字母和数字，如 `zhangsan2026`）
5. 点击 **"Continue"**（继续）
6. GitHub 会问一些问题，随便选即可，然后点 **"Continue"**
7. **去您的邮箱查收验证邮件**，点击验证链接
8. 🎉 注册完成！

---

## 第二步：创建仓库（存放代码的地方）

1. 登录 GitHub 后，点击右上角的 **+** 号 → 选择 **"New repository"**
2. **填写信息**：
   - Repository name（仓库名）：输入 `WatermarkCamera`
   - Description（描述）：可以填 "水印相机APP"（可选）
   - 选择 **Public**（公开）或 **Private**（私有）
3. **重要**：**不要**勾选 "Add a README file"
4. 点击最下方绿色按钮 **"Create repository"**
5. ✅ 仓库创建成功！

---

## 第三步：上传代码文件

### 方法 A：网页直接上传（最简单）

1. 回到您刚创建的仓库页面（如 `https://github.com/您的用户名/WatermarkCamera`）
2. 点击蓝色大按钮 **"uploading an existing file"**
3. **准备上传所有文件**：

   📁 打开电脑上的 `水印相机` 文件夹，选中以下所有文件和文件夹：
   - `.github` 文件夹
   - `app` 文件夹
   - `gradle` 文件夹
   - `build.gradle.kts` 文件
   - `settings.gradle.kts` 文件
   - `gradle.properties` 文件
   - `gradlew` 文件
   - `gradlew.bat` 文件
   - `proguard-rules.pro` 文件
   
   **⚠️ 不要上传**：`main.cpp` 文件（不需要）

4. **拖拽**选中的文件到浏览器的上传区域
5. 向下滚动，点击绿色按钮 **"Commit changes"**
6. ✅ 上传完成！

### 方法 B：使用 GitHub Desktop（推荐，支持子文件夹）

1. 下载 GitHub Desktop：**https://desktop.github.com/**
2. 安装并打开 GitHub Desktop
3. 点击 **"File"** → **"Add Local Repository"**
4. 选择 `水印相机` 文件夹，点击 **"Add Repository"**
5. 点击 **"Publish repository"**（发布到 GitHub）
6. 填写仓库名 `WatermarkCamera`，选择可见性
7. 点击 **"Publish Repository"**
8. ✅ 上传完成！

---

## 第四步：触发在线编译

1. 在 GitHub 上打开您的仓库页面
2. 点击顶部的 **Actions** 标签（在 Code 旁边）
3. 左侧会看到工作流列表，点击 **"Build APK"**
4. 右侧会出现一个 **"Run workflow"** 按钮（绿色）
5. 点击 **"Run workflow"**，再点击确认按钮
6. 稍等片刻（约 1-3 分钟），工作流开始运行
7. 📊 查看编译状态：
   - 🟡 黄色旋转图标 = 正在编译
   - 🟢 绿色对勾 ✅ = 编译成功
   - 🔴 红色 ❌ = 编译失败（如果失败，点击查看错误信息告诉我）

---

## 第五步：下载 APK 安装包

1. 当工作流显示 ✅ 绿色对勾后，点击该次运行的记录
2. 在页面下方找到 **"Artifacts"**（制品）区域
3. 您会看到两个文件：
   - 📦 **debug-apk**（调试版，体积较大）
   - 📦 **release-apk**（正式版，体积较小，推荐使用）
4. **点击 `release-apk` 下载**
5. 下载的是一个 ZIP 压缩包，解压后得到 `app-release.apk`

---

## 第六步：安装到手机

### 方法 A：USB 数据线（最直接）

1. 用 USB 数据线连接手机和电脑
2. 在手机上：设置 → 开发者选项 → 打开 **"USB 调试"**
3. 将 `app-release.apk` 复制到手机任意位置
4. 在手机文件管理器中点击该 APK
5. 允许"未知来源"安装（如果系统提示）
6. 点击安装 → 🎉 完成！

### 方法 B：通过微信/QQ传输

1. 将 APK 发送到自己的微信文件传输助手
2. 在手机微信中点击保存文件
3. 在手机文件管理器中找到并点击安装
4. 🎉 完成！

### 方法 C：通过云盘

1. 将 APK 上传到百度网盘/腾讯微云等
2. 在手机上下载并安装
3. 🎉 完成！

---

## 🎁 完成后

安装成功后，打开应用：
1. 首次使用需授权：点击"允许"相机、定位等权限
2. 授权后即可使用水印相机功能：
   - 📸 点击大按钮拍照
   - 🔄 点击绿色按钮刷新位置/天气
   - 🔴 点击红色按钮切换前后摄像头
3. 照片会自动保存到相册的 "WatermarkCamera" 文件夹

---

## ❓ 常见问题

### Q: 上传文件时提示"文件过大"？
A: 单个文件最大 25MB。如果是 `.gradle` 文件夹内的缓存文件，直接忽略即可。

### Q: 编译失败怎么办？
A: 点击红色 ❌ 标记，查看错误信息，常见原因：
- 文件不完整 → 重新上传
- 网络问题 → 再次点击 "Run workflow" 重试

### Q: 安装时提示"解析包时出现问题"？
A: 可能下载的是 debug 版或文件损坏，重新下载 release-apk。

### Q: 应用打开后照片是黑色的？
A: 确保已授权相机权限。部分手机需手动在设置中开启相机权限。

---

## 📝 总结流程

```
注册GitHub → 创建仓库 → 上传代码 → 点击Run workflow → 等待编译 → 下载APK → 安装到手机
```

祝您使用愉快！🎉